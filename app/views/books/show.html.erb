<% if flash[:notice] %>
	<div class="flash">
		<%= flash[:notice] %>
	</div>
<% end %>

<h1>Title</h1>
<%= @book.title %>
<p>Body</p>
<%= @book.body %>

<span><%= link_to "Edit", edit_book_path(@book.id) %></span>

<span><%= link_to "Back", books_path %></span>

