<h1>Editing Book</h1>

<%= form_for(@book) do |f|%>

	<h4>Tittle</h4>
	<%= f.text_field :title%>

	<h4>Body</h4>
	<%= f.text_area :body%>

	<%= f.submit 'Update Book'%>
<% end %>

<span><%= link_to "Show", book_path(@book.id) %></span>
<span><%= link_to "Back", books_path %></span>